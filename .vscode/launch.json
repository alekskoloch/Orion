{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug Orion (Clang/LLDB)",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/build/main.exe",
            "args": [],
            "cwd": "${workspaceFolder}/build",
            "stopOnEntry": false,
            "console": "integratedTerminal",
            "preLaunchTask": "Build Game"
        }
    ]
}